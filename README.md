# The Globetrotters

## How to set up
To set up the app, clone it to your local machine.

Next run 
```bash
npm install
```

Then set up a .env.local file following the .env.example file

## How to run the app
To start the app, run
```bash
npm run dev
```

Then connect to http://localhost:3000 to view the app